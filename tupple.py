# a=(1,67,  )
# print(type(a))  # Checking the type of the tuple
# no = a.count("idi")
# print(no)  # Printing the count of "idi" in the tuple
# i = a.index("pranjal")
# print(i)  # Printing the index of the first occurrence of "idi"
# b=(2,3,4,5  )
# concatenated_tuple = a + b  # Concatenating two tuples
# print(concatenated_tuple)  # Printing the concatenated tuple
# sorted_tuple = tuple(sorted(concatenated_tuple))  # Sorting the concatenated tuple
# print(sorted_tuple)  # Printing the sorted tuple
# print(5 in sorted_tuple)  # Checking if 5 is in the sorted tuple
# print(10 in sorted_tuple)  # Checking if 10 is in the sorted tuple
# print(len(sorted_tuple))  # Printing the length of the sorted tuple
# a,b,c = 1, 2, 3  # Unpacking values into variables
# print(a, b, c)  # Printing the unpacked values
a=(1, 0, 3, 0, 6, 0)
no = a.count(0)
print(no)  # Printing the count of 0 in the tuple